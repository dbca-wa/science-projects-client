apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spms-media-data
spec:
  storageClassName: azurefile-csi-retain
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
---
apiVersion: v1
kind: PersistentVolume
metadata:
  annotations:
    pv.kubernetes.io/provisioned-by: blob.csi.azure.com
  name: pv-spms-media-data-blob
spec:
  capacity:
    storage: 16Gi
  accessModes:
    - ReadWriteMany
  storageClassName: azureblob-fuse-retain
  mountOptions:
    - -o allow_other
    - --file-cache-timeout-in-seconds=120
  csi:
    driver: blob.csi.azure.com
    volumeHandle: bcsspms_spms-website-test
    volumeAttributes:
      storageAccount: bcsspms
      containerName: spms-website-test
    nodeStageSecretRef:
      name: spms-azure-storage-secret-test
      namespace: spms
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-spms-media-data-blob
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 16Gi
  volumeName: pv-spms-media-data-blob-test
  storageClassName: azureblob-fuse-retain
