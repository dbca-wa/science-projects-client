// User-related TypeScript types
// Note: Full type extraction from src/shared/types/index.d.ts will be completed during consolidation phase

// Re-export user types from shared for now
// These will be moved here during the consolidation phase
export type {
  IMiniUser,
  // Add other user-related types as they are identified
} from "@/shared/types";
