# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# =============================================================================

# API Base URL for development
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Sentry DSN for error tracking (optional - leave empty to disable)
# Format: https://<key>@<org>.ingest.us.sentry.io/<project>
# Get your DSN from: https://sentry.io/settings/projects/
VITE_SENTRY_DSN=

# Sentry environment (development, staging, production)
VITE_SENTRY_ENVIRONMENT=development

# Enable Sentry debug mode in development (optional)
# VITE_SENTRY_DEBUG=true

# =============================================================================
# PRODUCTION BUILD VARIABLES (set by CI/CD)
# =============================================================================
# These are typically set during Docker build by GitHub Actions
# You can override them for local production builds

# VITE_PRODUCTION_BACKEND_API_URL=https://your-api.example.com/api/v1/
# VITE_VERSION=1.0.0
