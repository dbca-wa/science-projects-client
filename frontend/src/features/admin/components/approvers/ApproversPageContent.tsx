import { DivisionalApproversTab } from "./DivisionalApproversTab";

/**
 * Content for the Divisional Approvers admin page.
 * Renders the divisional approvers tab directly without a tabbed layout.
 */
export const ApproversPageContent = () => {
	return <DivisionalApproversTab />;
};
