/**
 * Document Types
 *
 * Re-exports document types from shared types.
 * Document types are defined in shared because they're used across features.
 */

export type { IMainDoc } from "@/shared/types/document.types";

export type { DocumentType } from "@/shared/utils/document.utils";
