import { PageTransition } from "@/shared/components/PageTransition";

const UserGuide = () => {
	return (
		<PageTransition>
			<div>
				<span>UserGuide</span>
			</div>
		</PageTransition>
	);
};

export default UserGuide;
